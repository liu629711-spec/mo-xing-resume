# 后台 CMS 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为墨行个人介绍网站接入 Decap CMS 可视化后台，让作者在线增删改各板块内容并自动部署生效；运营碑石增强封面图、演示视频、富文本描述、数据指标。

**架构：** 纯静态站点加 `admin/` 目录（Decap CMS 入口 + 配置），内容仍存 `data/*.json`。后台保存 = git commit → Netlify 自动重新部署。前端加 `draft` 草稿过滤与视频识别纯函数。

**技术栈：** Decap CMS（CDN）、marked.js（markdown 渲染，CDN）、Vitest（纯函数 TDD）、现有 ES Modules + Three.js + GSAP 不变。

---

## 文件结构

**创建：**
- `js/utils/video.js` — 视频链接识别纯函数（mp4 / B 站 / YouTube）
- `js/utils/draft.js` — 草稿过滤纯函数
- `admin/admin.html` — Decap CMS 后台入口
- `admin/config.yml` — Decap CMS collection 字段配置
- `tests/utils/video.test.js` — 视频识别单测
- `tests/utils/draft.test.js` — 草稿过滤单测
- `docs/部署配置指南.md` — GitHub + Netlify Identity + Git Gateway 配置步骤

**修改：**
- `js/core/data-schema.js` — `validateGames` 扩展可选字段（video/description/metrics/draft），各 list validator 兼容 draft
- `js/core/data-loader.js` — 加载后对 list 类数据调用 `filterDraft`
- `js/sections/games.js` — 详情面板增强：封面大图、视频播放器、markdown 描述、指标卡
- `index.html` — 引入 marked.js CDN
- `data/games.json` — 给现有碑石补 `video`/`description`/`metrics`/`draft` 字段（空值占位，验证渲染）
- `tests/data-schema.test.js` — 补 games 扩展字段用例

---

## 任务 1：视频链接识别纯函数（TDD）

**文件：**
- 创建：`js/utils/video.js`
- 测试：`tests/utils/video.test.js`

- [ ] **步骤 1：编写失败的测试**

`tests/utils/video.test.js`：

```javascript
import { describe, it, expect } from 'vitest';
import { resolveVideo } from '../js/utils/video.js';

describe('resolveVideo', () => {
  it('空字符串返回 null', () => {
    expect(resolveVideo('')).toBeNull();
    expect(resolveVideo('   ')).toBeNull();
  });

  it('mp4 直链返回 type=video', () => {
    const r = resolveVideo('https://oss.example.com/demo.mp4');
    expect(r.type).toBe('video');
    expect(r.src).toBe('https://oss.example.com/demo.mp4');
  });

  it('webm 直链返回 type=video', () => {
    const r = resolveVideo('https://cdn.x.cn/a.webm');
    expect(r.type).toBe('video');
  });

  it('B 站 BV 链接返回 type=bilibili + embed', () => {
    const r = resolveVideo('https://www.bilibili.com/video/BV1xx411c7mD');
    expect(r.type).toBe('bilibili');
    expect(r.embed).toContain('player.bilibili.com');
    expect(r.embed).toContain('BV1xx411c7mD');
  });

  it('B 站短链接 b23.tv 返回 type=bilibili（无法解析 BV 时降级 null）', () => {
    // b23.tv 短链无 BV 号，无法直接 embed，返回 null
    expect(resolveVideo('https://b23.tv/abc123')).toBeNull();
  });

  it('YouTube 普通链接返回 type=youtube + embed', () => {
    const r = resolveVideo('https://www.youtube.com/watch?v=dQw4w9WgXcQ');
    expect(r.type).toBe('youtube');
    expect(r.embed).toContain('youtube.com/embed/dQw4w9WgXcQ');
  });

  it('YouTube 短链接返回 type=youtube', () => {
    const r = resolveVideo('https://youtu.be/dQw4w9WgXcQ');
    expect(r.type).toBe('youtube');
    expect(r.embed).toContain('youtube.com/embed/dQw4w9WgXcQ');
  });

  it('未知链接返回 null', () => {
    expect(resolveVideo('https://example.com/page')).toBeNull();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/utils/video.test.js`
预期：FAIL，报错 `Cannot find module '../js/utils/video.js'`

- [ ] **步骤 3：编写最少实现代码**

`js/utils/video.js`：

```javascript
// 视频链接识别：把作者填的链接转成可嵌入的播放器描述。
// 返回 { type, src? , embed? } 或 null。

const VIDEO_EXT = /\.(mp4|webm|ogg|mov)$/i;

export function resolveVideo(url) {
  if (typeof url !== 'string') return null;
  const u = url.trim();
  if (!u) return null;

  // 1. 直链视频文件
  if (VIDEO_EXT.test(u)) {
    return { type: 'video', src: u };
  }

  // 2. B 站：提取 BV 号
  const bv = u.match(/bilibili\.com\/video\/(BV[\w]+)/i);
  if (bv) {
    const id = bv[1];
    return {
      type: 'bilibili',
      embed: `https://player.bilibili.com/player.html?bvid=${id}&high_quality=1&autoplay=0`,
    };
  }

  // 3. YouTube：普通链接或短链
  const ytLong = u.match(/youtube\.com\/watch\?v=([\w-]+)/i);
  const ytShort = u.match(/youtu\.be\/([\w-]+)/i);
  const ytId = (ytLong && ytLong[1]) || (ytShort && ytShort[1]);
  if (ytId) {
    return {
      type: 'youtube',
      embed: `https://www.youtube.com/embed/${ytId}`,
    };
  }

  return null;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/utils/video.test.js`
预期：PASS（全部用例通过）

- [ ] **步骤 5：Commit**

```bash
git add js/utils/video.js tests/utils/video.test.js
git commit -m "feat(video): 添加视频链接识别纯函数（mp4/B站/YouTube）"
```

---

## 任务 2：草稿过滤纯函数（TDD）

**文件：**
- 创建：`js/utils/draft.js`
- 测试：`tests/utils/draft.test.js`

- [ ] **步骤 1：编写失败的测试**

`tests/utils/draft.test.js`：

```javascript
import { describe, it, expect } from 'vitest';
import { filterDraft } from '../js/utils/draft.js';

describe('filterDraft', () => {
  it('过滤掉 draft:true 的条目', () => {
    const arr = [
      { name: 'a', draft: false },
      { name: 'b', draft: true },
      { name: 'c' },
    ];
    expect(filterDraft(arr)).toEqual([
      { name: 'a', draft: false },
      { name: 'c' },
    ]);
  });

  it('全部发布时全部保留', () => {
    const arr = [{ a: 1 }, { a: 2 }];
    expect(filterDraft(arr)).toHaveLength(2);
  });

  it('全部草稿时返回空数组', () => {
    expect(filterDraft([{ x: 1, draft: true }])).toEqual([]);
  });

  it('非数组返回空数组', () => {
    expect(filterDraft(null)).toEqual([]);
    expect(filterDraft(undefined)).toEqual([]);
    expect(filterDraft({})).toEqual([]);
  });

  it('不修改原数组', () => {
    const arr = [{ a: 1, draft: true }, { a: 2 }];
    filterDraft(arr);
    expect(arr).toHaveLength(2);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/utils/draft.test.js`
预期：FAIL，报错 `Cannot find module '../js/utils/draft.js'`

- [ ] **步骤 3：编写最少实现代码**

`js/utils/draft.js`：

```javascript
// 草稿过滤：返回 draft 不为 true 的条目副本。
// 作者在后台用 draft 开关临时隐藏某条而不删除。
export function filterDraft(arr) {
  if (!Array.isArray(arr)) return [];
  return arr.filter((item) => !item || item.draft !== true);
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/utils/draft.test.js`
预期：PASS

- [ ] **步骤 5：Commit**

```bash
git add js/utils/draft.js tests/utils/draft.test.js
git commit -m "feat(draft): 添加草稿过滤纯函数"
```

---

## 任务 3：扩展 games schema + data-loader 草稿过滤（TDD）

**文件：**
- 修改：`js/core/data-schema.js:65-77`（validateGames）
- 修改：`js/core/data-loader.js`（导入并应用 filterDraft）
- 修改：`tests/data-schema.test.js`（补 games 扩展字段用例）

- [ ] **步骤 1：编写失败的测试**

在 `tests/data-schema.test.js` 末尾追加：

```javascript
describe('games schema 扩展字段', () => {
  const base = { name: 'g', type: 'RPG', period: '2024', role: 'r', cover: '', note: 'n' };

  it('带 video/description/metrics/draft 可选字段通过', () => {
    const g = {
      ...base,
      video: 'https://oss.x.com/a.mp4',
      description: '## 亮点\n运营心得...',
      metrics: [{ label: 'DAU', value: '+120%', unit: '' }],
      draft: false,
    };
    expect(validateGames([g])).toBe(true);
  });

  it('只有基础字段仍通过（向后兼容）', () => {
    expect(validateGames([base])).toBe(true);
  });

  it('video 非字符串失败', () => {
    expect(validateGames([{ ...base, video: 123 }])).toBe(false);
  });

  it('description 非字符串失败', () => {
    expect(validateGames([{ ...base, description: [] }])).toBe(false);
  });

  it('metrics 非数组失败', () => {
    expect(validateGames([{ ...base, metrics: 'x' }])).toBe(false);
  });

  it('draft 非布尔失败', () => {
    expect(validateGames([{ ...base, draft: 'yes' }])).toBe(false);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`npx vitest run tests/data-schema.test.js`
预期：FAIL，`带 video/description/metrics/draft 可选字段通过` 等用例失败（当前 validator 不识别新字段，非字符串字段不会被拦截）

- [ ] **步骤 3：编写最少实现代码**

修改 `js/core/data-schema.js` 的 `validateGames`（第 65-77 行）替换为：

```javascript
export function validateGames(g) {
  if (!is.arr(g) || g.length === 0) return false;
  for (const game of g) {
    if (!is.obj(game)) return false;
    if (!is.str(game.name) || !game.name) return false;
    if (!is.str(game.type)) return false;
    if (!is.str(game.period)) return false;
    if (!is.str(game.role)) return false;
    if (!is.str(game.cover)) return false;
    if (!is.str(game.note)) return false;
    // 可选扩展字段（视频/富文本/指标/草稿）
    if ('video' in game && !is.str(game.video)) return false;
    if ('description' in game && !is.str(game.description)) return false;
    if ('metrics' in game && !is.arr(game.metrics)) return false;
    if ('draft' in game && typeof game.draft !== 'boolean') return false;
    if ('nda' in game && typeof game.nda !== 'boolean') return false;
  }
  return true;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`npx vitest run tests/data-schema.test.js`
预期：PASS（含新增用例与原有用例）

- [ ] **步骤 5：在 data-loader 集成草稿过滤**

修改 `js/core/data-loader.js`，在顶部 import 后追加：

```javascript
import { filterDraft } from '../utils/draft.js';
```

将 `loadAllData` 改为（对 list 类数据应用 filterDraft）：

```javascript
export async function loadAllData() {
  const [profile, skills, projects, metrics, games, timeline, contact] = await Promise.all([
    load('profile', validateProfile),
    load('skills', validateSkills),
    load('projects', validateProjects),
    load('metrics', validateMetrics),
    load('games', validateGames),
    load('timeline', validateTimeline),
    load('contact', validateContact),
  ]);
  return {
    profile,
    skills: filterDraft(skills),
    projects: filterDraft(projects),
    metrics,
    games: filterDraft(games),
    timeline: filterDraft(timeline),
    contact,
  };
}
```

- [ ] **步骤 6：运行全部测试验证通过**

运行：`npm test`
预期：PASS（所有测试文件全绿）

- [ ] **步骤 7：Commit**

```bash
git add js/core/data-schema.js js/core/data-loader.js tests/data-schema.test.js
git commit -m "feat(schema): games 扩展视频/富文本/指标/草稿字段 + loader 草稿过滤"
```

---

## 任务 4：运营碑石详情面板增强（视频/富文本/指标卡）

**文件：**
- 修改：`js/sections/games.js`（详情面板 + 卡片渲染）
- 修改：`index.html`（引入 marked.js CDN）
- 修改：`data/games.json`（给现有碑石补空占位字段验证渲染）
- 修改：`css/sections.css`（视频与指标卡样式）

- [ ] **步骤 1：引入 marked.js CDN**

在 `index.html` 第 74 行 d3 之后追加一行：

```html
  <script src="https://cdn.jsdelivr.net/npm/marked@12.0.0/marked.min.js"></script>
```

- [ ] **步骤 2：给现有 games.json 补占位字段**

修改 `data/games.json`，给第一个非 NDA 游戏补充字段验证渲染（保留现有数据，追加字段）：

```json
[
  {
    "name": "幻境探险",
    "type": "MMORPG",
    "period": "2024.01 - 至今",
    "role": "运营负责人",
    "cover": "",
    "note": "主导版本节奏与数据增长，半年内 DAU 翻倍。",
    "video": "",
    "description": "## 核心亮点\n\n- 重构新手引导漏斗，次日留存 **+8pp**\n- 设计通行证+月卡体系，ARPU **+22%**\n- 主导春节/周年庆大促，单活动流水破千万\n\n## 运营节奏\n\n按版本周期编排活动，数据驱动复盘迭代。",
    "metrics": [
      { "label": "DAU", "value": "+120%", "unit": "" },
      { "label": "留存", "value": "+8", "unit": "pp" },
      { "label": "流水", "value": "+35%", "unit": "" }
    ],
    "draft": false
  },
  {
    "name": "星海征途",
    "type": "SLG",
    "period": "2022.06 - 2023.12",
    "role": "高级运营",
    "cover": "",
    "note": "搭建赛季制活动体系，月活环比+60%。",
    "video": "",
    "description": "",
    "metrics": [],
    "draft": false
  },
  {
    "name": "萌宠小镇",
    "type": "模拟经营",
    "period": "2021.03 - 2022.05",
    "role": "运营专员",
    "cover": "",
    "note": "建立数据日报机制，节日活动驱动稳定增长。",
    "video": "",
    "description": "",
    "metrics": [],
    "draft": false
  },
  {
    "name": "未上线项目",
    "type": "NDA",
    "period": "2025",
    "role": "运营顾问",
    "cover": "",
    "note": "因保密协议不便展示",
    "video": "",
    "description": "",
    "metrics": [],
    "nda": true,
    "draft": false
  }
]
```

- [ ] **步骤 3：重写 games.js 详情面板**

修改 `js/sections/games.js`，在文件顶部 import 区追加：

```javascript
import { resolveVideo } from '../utils/video.js';
```

替换 `openGameDetail` 函数（第 43-69 行）为：

```javascript
function openGameDetail(g) {
  const overlay = document.getElementById('project-detail-overlay');
  if (!overlay) return;

  const video = resolveVideo(g.video || '');
  const videoHtml = video ? renderVideo(video) : '';
  const descHtml = (g.description && typeof marked !== 'undefined')
    ? `<div class="detail-section"><h4>运营心得</h4><div class="detail-markdown">${marked.parse(g.description)}</div></div>`
    : '';
  const metricsHtml = (g.metrics && g.metrics.length)
    ? `<div class="detail-section"><h4>关键数据</h4><div class="detail-metrics">${g.metrics.map(renderMetric).join('')}</div></div>`
    : '';
  const coverHtml = g.cover
    ? `<div class="detail-cover" style="background-image:url('${escapeHtml(g.cover)}')"></div>`
    : '';

  overlay.innerHTML = `
    <div class="detail-scroll" role="document">
      <button class="detail-close" aria-label="关闭">收卷</button>
      ${coverHtml}
      <h3 class="detail-title">${escapeHtml(g.name)}</h3>
      <div class="detail-meta">${escapeHtml(g.type)} · ${escapeHtml(g.period)} · ${escapeHtml(g.role)}</div>
      ${videoHtml}
      ${descHtml}
      ${metricsHtml}
      <div class="detail-section">
        <h4>一句话亮点</h4>
        <p class="ink-text" style="line-height:1.9">${escapeHtml(g.note)}</p>
      </div>
    </div>
  `;
  overlay.classList.add('is-open');
  overlay.setAttribute('aria-hidden', 'false');
  const close = () => {
    overlay.classList.remove('is-open');
    overlay.setAttribute('aria-hidden', 'true');
    overlay.innerHTML = '';
    document.removeEventListener('keydown', onKey);
  };
  const onKey = (e) => { if (e.key === 'Escape') close(); };
  overlay.querySelector('.detail-close').addEventListener('click', close);
  overlay.addEventListener('click', (e) => { if (e.target === overlay) close(); });
  document.addEventListener('keydown', onKey);
}

function renderVideo(v) {
  if (v.type === 'video') {
    return `<div class="detail-video"><video src="${escapeHtml(v.src)}" controls preload="metadata" playsinline></video></div>`;
  }
  if (v.type === 'bilibili' || v.type === 'youtube') {
    return `<div class="detail-video"><iframe src="${escapeHtml(v.embed)}" frameborder="0" allow="accelerated-bytes; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></div>`;
  }
  return '';
}

function renderMetric(m) {
  return `<div class="metric-card"><div class="metric-value">${escapeHtml(String(m.value))}<span class="metric-unit">${escapeHtml(m.unit || '')}</span></div><div class="metric-label">${escapeHtml(m.label)}</div></div>`;
}
```

- [ ] **步骤 4：追加详情面板视频/指标卡样式**

在 `css/sections.css` 末尾追加：

```css
/* 运营碑石详情：视频 + 指标卡 */
.detail-cover {
  width: 100%;
  height: 28vh;
  min-height: 180px;
  max-height: 320px;
  background-size: cover;
  background-position: center;
  border-radius: 6px;
  margin-bottom: 2vh;
  filter: grayscale(0.2) contrast(1.05);
}
.detail-video {
  position: relative;
  width: 100%;
  padding-bottom: 56.25%;
  margin-bottom: 2vh;
  border-radius: 6px;
  overflow: hidden;
  background: #000;
}
.detail-video video,
.detail-video iframe {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  border: 0;
}
.detail-markdown {
  line-height: 1.9;
  color: var(--ink, #1a1a1a);
}
.detail-markdown h2 { font-size: 1.2em; margin: 1em 0 0.4em; }
.detail-markdown ul { padding-left: 1.4em; }
.detail-markdown li { margin: 0.3em 0; }
.detail-markdown strong { color: var(--gold, #b8860b); }
.detail-metrics {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
  gap: 1.2vh 2vw;
  margin-top: 1vh;
}
.metric-card {
  padding: 1.6vh 2vw;
  border: 1px solid rgba(26,26,26,0.15);
  border-radius: 6px;
  background: rgba(255,255,255,0.4);
  text-align: center;
}
.metric-value {
  font-size: clamp(20px, 3vw, 30px);
  font-weight: 600;
  color: var(--gold, #b8860b);
  font-family: 'Cormorant Garamond', 'Noto Serif SC', serif;
}
.metric-unit { font-size: 0.6em; margin-left: 2px; }
.metric-label { font-size: 13px; opacity: 0.7; margin-top: 4px; }
```

- [ ] **步骤 5：手动验证渲染**

运行：`npx serve .`（或 `npm run dev`）→ 浏览器打开 → 滚到"运营碑石" → 点击"幻境探险"碑石 → 确认详情面板显示：封面占位（空则不显示）、标题/meta、运营心得 markdown 渲染（含加粗金色）、关键数据 3 张指标卡、一句话亮点。
预期：无 JS 报错，markdown 正确渲染，指标卡网格显示。

- [ ] **步骤 6：Commit**

```bash
git add js/sections/games.js index.html data/games.json css/sections.css
git commit -m "feat(games): 碑石详情面板增强——视频播放器/富文本/指标卡"
```

---

## 任务 5：Decap CMS 后台接入（admin.html + config.yml）

**文件：**
- 创建：`admin/admin.html`
- 创建：`admin/config.yml`

- [ ] **步骤 1：创建后台入口页**

`admin/admin.html`：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>墨行简历 · 内容后台</title>
  <link rel="stylesheet" href="https://unpkg.com/netlify-cms@2.10.192/dist/netlify-cms.css" />
</head>
<body>
  <script src="https://unpkg.com/netlify-cms@2.10.192/dist/netlify-cms.js"></script>
  <script>
    // 注册中文界面（Decap CMS 内置 zh-cn）
    if (window.netlifyIdentity) {
      window.netlifyIdentity.on('init', (user) => {
        if (!user) {
          window.netlifyIdentity.on('login', () => location.reload());
        }
      });
    }
  </script>
</body>
</html>
```

- [ ] **步骤 2：创建 CMS 配置**

`admin/config.yml`：

```yaml
backend:
  name: git-gateway
  branch: main
  commit_messages:
    create: 'cms: 新建 {{collection}} "{{slug}}"'
    update: 'cms: 更新 {{collection}} "{{slug}}"'
    delete: 'cms: 删除 {{collection}} "{{slug}}"'
publish_mode: editorial_workflow
media_folder: 'uploads'
public_folder: '/uploads'
site_url: /

collections:
  # 关于（单文件）
  - name: 'profile'
    label: '关于 · 个人介绍'
    file: 'data/profile.json'
    create: false
    delete: false
    fields:
      - { label: '姓名', name: 'name', widget: 'string' }
      - { label: '标语', name: 'tagline', widget: 'string' }
      - { label: '印章字', name: 'seal', widget: 'string' }
      - { label: '头像', name: 'avatar', widget: 'image', required: false }
      - { label: '自我介绍', name: 'summary', widget: 'list', field: { label: '一句话', name: 'text', widget: 'string' } }

  # 技艺（列表）
  - name: 'skills'
    label: '技艺 · 技能矩阵'
    folder: 'data/skills'
    create: true
    delete: true
    extension: 'json'
    format: 'json'
    slug: '{{category}}'
    fields:
      - { label: '分类', name: 'category', widget: 'string' }
      - label: '技能项'
        name: 'items'
        widget: 'list'
        fields:
          - { label: '技能名', name: 'name', widget: 'string' }
          - { label: '熟练度(0-1)', name: 'level', widget: 'number', value_type: 'float', min: 0, max: 1, step: 0.05 }
          - { label: '技能故事', name: 'story', widget: 'text' }
      - { label: '草稿(隐藏)', name: 'draft', widget: 'boolean', default: false, required: false }

  # 项目（列表）
  - name: 'projects'
    label: '项目 · 案例经历'
    folder: 'data/projects'
    create: true
    delete: true
    extension: 'json'
    format: 'json'
    slug: '{{name}}'
    fields:
      - { label: '项目名', name: 'name', widget: 'string' }
      - { label: '时间段', name: 'period', widget: 'string' }
      - { label: '角色', name: 'role', widget: 'string' }
      - { label: '项目图', name: 'image', widget: 'image', required: false }
      - { label: '影响权重(0-1)', name: 'impact', widget: 'number', value_type: 'float', min: 0, max: 1, step: 0.05 }
      - label: '指标'
        name: 'metrics'
        widget: 'object'
        fields:
          - { label: 'DAU', name: 'dau', widget: 'string', required: false }
          - { label: '留存', name: 'retention', widget: 'string', required: false }
          - { label: '营收', name: 'revenue', widget: 'string', required: false }
      - { label: '职责', name: 'duties', widget: 'list', field: { label: '条目', name: 'text', widget: 'string' } }
      - { label: '关键动作', name: 'actions', widget: 'list', field: { label: '条目', name: 'text', widget: 'string' } }
      - { label: '草稿(隐藏)', name: 'draft', widget: 'boolean', default: false, required: false }

  # 数据成河（单文件）
  - name: 'metrics'
    label: '数据成河 · 数据看板'
    file: 'data/metrics.json'
    create: false
    delete: false
    fields:
      - label: '高亮指标'
        name: 'highlights'
        widget: 'list'
        fields:
          - { label: '标签', name: 'label', widget: 'string' }
          - { label: '数值', name: 'value', widget: 'string' }
          - { label: '单位', name: 'unit', widget: 'string', required: false }
      - label: 'DAU 曲线'
        name: 'dauCurve'
        widget: 'list'
        required: false
        fields:
          - { label: '月份', name: 'month', widget: 'string' }
          - { label: 'DAU', name: 'dau', widget: 'number' }
      - label: '用户来源'
        name: 'userSources'
        widget: 'list'
        required: false
        fields:
          - { label: '标签', name: 'label', widget: 'string' }
          - { label: '占比', name: 'value', widget: 'number' }
      - label: 'ROI 柱状图'
        name: 'roiBars'
        widget: 'list'
        required: false
        fields:
          - { label: '活动', name: 'activity', widget: 'string' }
          - { label: 'ROI', name: 'roi', widget: 'number', value_type: 'float' }

  # 运营碑石（列表 · 含视频增强）
  - name: 'games'
    label: '运营碑石 · 游戏作品'
    folder: 'data/games'
    create: true
    delete: true
    extension: 'json'
    format: 'json'
    slug: '{{name}}'
    fields:
      - { label: '游戏名', name: 'name', widget: 'string' }
      - { label: '类型', name: 'type', widget: 'string' }
      - { label: '时间段', name: 'period', widget: 'string' }
      - { label: '角色', name: 'role', widget: 'string' }
      - { label: '封面图', name: 'cover', widget: 'image', required: false }
      - { label: '一句话亮点', name: 'note', widget: 'string' }
      - { label: '演示视频链接', name: 'video', widget: 'string', required: false, hint: '支持 mp4/webm 直链、B 站视频页链接、YouTube 链接' }
      - { label: '详细描述(Markdown)', name: 'description', widget: 'markdown', required: false }
      - label: '关键数据指标'
        name: 'metrics'
        widget: 'list'
        required: false
        fields:
          - { label: '标签', name: 'label', widget: 'string' }
          - { label: '数值', name: 'value', widget: 'string' }
          - { label: '单位', name: 'unit', widget: 'string', required: false }
      - { label: 'NDA 保密', name: 'nda', widget: 'boolean', default: false, required: false }
      - { label: '草稿(隐藏)', name: 'draft', widget: 'boolean', default: false, required: false }

  # 时间轴（列表）
  - name: 'timeline'
    label: '时间轴 · 经历'
    folder: 'data/timeline'
    create: true
    delete: true
    extension: 'json'
    format: 'json'
    slug: '{{year}}-{{title}}'
    fields:
      - { label: '时间', name: 'year', widget: 'string' }
      - { label: '标题', name: 'title', widget: 'string' }
      - { label: '组织', name: 'org', widget: 'string' }
      - { label: '描述', name: 'desc', widget: 'text' }
      - { label: '印章字', name: 'stamp', widget: 'string' }
      - { label: '草稿(隐藏)', name: 'draft', widget: 'boolean', default: false, required: false }

  # 联系（单文件）
  - name: 'contact'
    label: '联系 · 联系方式'
    file: 'data/contact.json'
    create: false
    delete: false
    fields:
      - { label: '邮箱', name: 'email', widget: 'string' }
      - { label: '微信号', name: 'wechat', widget: 'string', required: false }
      - { label: '微信二维码', name: 'wechatQr', widget: 'image', required: false }
      - { label: '电话', name: 'phone', widget: 'string', required: false }
      - { label: 'LinkedIn', name: 'linkedin', widget: 'string', required: false }
      - { label: '脉脉', name: 'maimai', widget: 'string', required: false }
      - { label: '邀请语', name: 'invite', widget: 'text' }
```

> **注意：** Decap 的 `folder` collection 要求每个条目是独立文件。当前 `skills/projects/games/timeline` 是单 JSON 数组文件。有两种处理方式：
> - **方式 A（推荐，改前端）**：把数组文件拆成 `data/skills/<slug>.json` 单文件目录，前端 loader 改为读目录索引或合并。Decap 原生支持 folder collection。
> - **方式 B（保持单文件）**：用 Decap 的 `file` collection 把整个数组当一个字段管理，但表单体验差（要在单个 list widget 里增删）。
>
> 本计划步骤 3-4 采用**方式 A**：拆分数组文件为目录 + 前端 loader 聚合。下面任务 5B 处理拆分。

- [ ] **步骤 3：拆分 list 数组文件为目录**

把 `data/skills.json`、`data/projects.json`、`data/games.json`、`data/timeline.json` 各拆成目录：

对每个文件，每个数组元素写成 `data/<name>/<slug>.json`（单个对象，非数组），slug 用 `name`/`category`/`year-title` 等可读值。原 `data/<name>.json` 删除。

示例拆分后结构：
```
data/
  profile.json
  metrics.json
  contact.json
  skills/
    活动策划.json
    数据分析.json
    用户增长.json
    商业化.json
    工具.json
  projects/
    幻境探险.json
    星海征途.json
    萌宠小镇.json
  games/
    幻境探险.json
    星海征途.json
    萌宠小镇.json
    未上线项目.json
  timeline/
    2024.01-运营负责人.json
    2022.06-高级运营.json
    2021.03-运营专员.json
    2020.06-毕业.json
```

每个单文件内容是对应数组元素的对象（如 `data/skills/活动策划.json` 是 `{ "category": "活动策划", "items": [...] }`）。

- [ ] **步骤 4：改造 data-loader 聚合目录**

修改 `js/core/data-loader.js`，新增目录聚合加载。替换文件顶部到 `loadAllData` 之间为：

```javascript
// 数据加载：并行 fetch 所有 JSON，校验失败回退占位。
import {
  validateProfile, validateSkills, validateProjects,
  validateMetrics, validateGames, validateTimeline, validateContact,
} from './data-schema.js';
import { filterDraft } from '../utils/draft.js';

const FALLBACK = {
  profile: { name: '墨行', tagline: '游戏产品运营', summary: ['内容更新中'], seal: '墨' },
  skills: [{ category: '加载中', items: [{ name: '...', level: 0.5, story: '' }] }],
  projects: [],
  metrics: { highlights: [{ label: '数据', value: '—', unit: '' }], dauCurve: [], userSources: [], roiBars: [] },
  games: [],
  timeline: [],
  contact: { email: '', wechat: '', invite: '内容更新中' },
};

async function load(name, validator) {
  try {
    const res = await fetch(`data/${name}.json`);
    if (!res.ok) throw new Error(`${name}.json HTTP ${res.status}`);
    const data = await res.json();
    if (!validator(data)) {
      console.warn(`[data] ${name}.json schema 校验失败，使用占位`);
      return FALLBACK[name];
    }
    return data;
  } catch (e) {
    console.warn(`[data] 加载 ${name}.json 失败:`, e.message, '，使用占位');
    return FALLBACK[name];
  }
}

// 加载目录式 collection：读 data/<name>/index.json（由构建期生成或手动维护）
// 若不存在 index.json，则尝试读取 data/<name>.json（向后兼容单文件数组）
async function loadDir(name, validator) {
  try {
    const idxRes = await fetch(`data/${name}/index.json`);
    if (idxRes.ok) {
      const files = await idxRes.json();
      const items = await Promise.all(files.map((f) =>
        fetch(`data/${name}/${f}`).then((r) => r.json())
      ));
      const arr = items.filter(Boolean);
      if (validator(arr)) return filterDraft(arr);
      console.warn(`[data] ${name}/ 目录数据校验失败，使用占位`);
      return FALLBACK[name];
    }
    // 回退到单文件数组
    const data = await load(name, validator);
    return filterDraft(data);
  } catch (e) {
    console.warn(`[data] 加载 ${name} 目录失败:`, e.message);
    return FALLBACK[name];
  }
}

export async function loadAllData() {
  const [profile, skills, projects, metrics, games, timeline, contact] = await Promise.all([
    load('profile', validateProfile),
    loadDir('skills', validateSkills),
    loadDir('projects', validateProjects),
    load('metrics', validateMetrics),
    loadDir('games', validateGames),
    loadDir('timeline', validateTimeline),
    load('contact', validateContact),
  ]);
  return {
    profile,
    skills,
    projects,
    metrics,
    games,
    timeline,
    contact,
  };
}
```

为每个目录创建 `data/<name>/index.json`，列出文件名数组。示例 `data/games/index.json`：

```json
["幻境探险.json", "星海征途.json", "萌宠小镇.json", "未上线项目.json"]
```

同理创建 `data/skills/index.json`、`data/projects/index.json`、`data/timeline/index.json`。

> **Decap 行为说明：** Decap CMS 不会自动生成 index.json。每次后台增删条目后，作者需手动更新 index.json，或在任务 5C 中用一个 Netlify 构建插件自动生成。本计划采用构建期自动生成（任务 5C），避免作者手动维护。

- [ ] **步骤 5：手动验证本地渲染**

运行：`npx serve .` → 打开浏览器 → 确认七板块内容正常显示（与拆分前一致），运营碑石点击详情正常。
预期：数据加载无报错，内容与拆分前一致。

- [ ] **步骤 6：Commit**

```bash
git add admin/ data/ js/core/data-loader.js
git commit -m "feat(cms): 接入 Decap CMS 后台 + 拆分 list 为目录式 collection"
```

---

## 任务 5B：构建期自动生成目录索引

**文件：**
- 创建：`scripts/gen-index.mjs`
- 创建：`netlify.toml`（若不存在）

- [ ] **步骤 1：创建索引生成脚本**

`scripts/gen-index.mjs`：

```javascript
// 构建期扫描 data/<name>/ 目录，生成 index.json 文件列表。
// 让前端 data-loader 能聚合目录式 collection，无需手动维护索引。
import { readdir, writeFile } from 'node:fs/promises';
import { join } from 'node:path';

const DIRS = ['skills', 'projects', 'games', 'timeline'];

async function genIndex(dir) {
  const abs = join(process.cwd(), 'data', dir);
  let entries;
  try {
    entries = await readdir(abs);
  } catch (e) {
    console.warn(`[gen-index] 跳过 ${dir}/（目录不存在）: ${e.message}`);
    return;
  }
  const files = entries
    .filter((f) => f.endsWith('.json') && f !== 'index.json')
    .sort();
  await writeFile(join(abs, 'index.json'), JSON.stringify(files, null, 2));
  console.log(`[gen-index] ${dir}/index.json -> ${files.length} 个文件`);
}

await Promise.all(DIRS.map(genIndex));
```

- [ ] **步骤 2：配置 Netlify 构建钩子**

`netlify.toml`：

```toml
[build]
  command = "node scripts/gen-index.mjs"
  publish = "."

# admin 后台需要 Identity + Git Gateway，在 Netlify 控制台手动开启
# 详见 docs/部署配置指南.md
```

- [ ] **步骤 3：本地运行验证脚本**

运行：`node scripts/gen-index.mjs`
预期：输出 4 行 `[gen-index] <name>/index.json -> N 个文件`，且各 `data/<name>/index.json` 生成正确。

- [ ] **步骤 4：再次本地验证前端加载**

运行：`npx serve .` → 浏览器打开 → 确认内容正常。
预期：data-loader 通过 index.json 聚合目录，七板块内容正常显示。

- [ ] **步骤 5：更新 .gitignore 排除生成产物**

在 `.gitignore` 追加（若文件不存在则创建）：

```
data/*/index.json
```

- [ ] **步骤 6：Commit**

```bash
git add scripts/gen-index.mjs netlify.toml .gitignore
git commit -m "build(cms): 构建期自动生成目录索引，无需手动维护 index.json"
```

---

## 任务 6：部署配置指南文档

**文件：**
- 创建：`docs/部署配置指南.md`

- [ ] **步骤 1：编写部署指南**

`docs/部署配置指南.md`：

```markdown
# 部署配置指南 · 墨行简历后台

## 前置条件
- GitHub 账号
- Netlify 账号（可用 GitHub 登录）

## 步骤 1：项目推到 GitHub
1. 在 GitHub 新建仓库（建议私有：`mo-xing-resume`）
2. 本地项目初始化并推送：
   ```bash
   git init
   git add .
   git commit -m "init: 墨行个人介绍网站"
   git branch -M main
   git remote add origin git@github.com:<你的用户名>/mo-xing-resume.git
   git push -u origin main
   ```

## 步骤 2：Netlify 连接仓库
1. 登录 https://app.netlify.com
2. "Add new site" → "Import an existing project" → 选 GitHub → 选 `mo-xing-resume` 仓库
3. 构建配置（应自动读取 netlify.toml）：
   - Build command: `node scripts/gen-index.mjs`
   - Publish directory: `.`
4. 点 Deploy，等待首次部署完成

## 步骤 3：开启 Identity（登录系统）
1. Netlify 站点后台 → "Identity" 标签 → "Enable Identity"
2. Registration preferences：选 "Invite only"（只允许被邀请的人，即你自己）
3. 添加你自己：点 "Invite users" → 填你的邮箱 → 收邮件激活设密码

## 步骤 4：开启 Git Gateway（让后台能提交到仓库）
1. Netlify 站点后台 → "Identity" → "Services" → "Git Gateway" → "Enable"
2. 授权 Netlify 访问你的 GitHub 仓库

## 步骤 5：进入后台
1. 访问 `https://<你的站点>.netlify.app/admin/`
2. 用步骤 3 激活的账号登录
3. 看到 7 个板块集合，点进去可视化编辑
4. 改完点保存 → 自动 git commit → 触发 Netlify 重新构建 → 30-60 秒后线上生效

## 视频存储建议
录屏文件较大，不建议传仓库。推荐：
- **阿里云 OSS / 腾讯云 COS**：传 mp4 拿直链，填到碑石"演示视频链接"字段。费用极低（几毛/月），无广告，国内稳定。
- B 站：传后填视频页链接（有广告/推荐，不太适合理简历）
- 三种链接后台都支持，前端自动识别

## 常见问题
- **进不了后台**：确认 Identity 开了 + 账号已激活 + Git Gateway 已启用
- **保存后线上没更新**：Netlify 构建需 30-60 秒，看 Netlify 控制台 "Deploys" 是否在构建中
- **草稿不显示**：开了"草稿(隐藏)"开关的条目前端不渲染，关掉开关即恢复
```

- [ ] **步骤 2：Commit**

```bash
git add docs/部署配置指南.md
git commit -m "docs: 添加 GitHub+Netlify Identity 部署配置指南"
```

---

## 自检

**1. 规格覆盖度：**
- §3.1 profile → 任务 5 config.yml profile collection ✓
- §3.2 skills → 任务 5 skills collection + 任务 5B 索引 ✓
- §3.3 projects → 任务 5 projects collection ✓
- §3.4 metrics → 任务 5 metrics file collection ✓
- §3.5 games（含视频增强）→ 任务 1 视频识别 + 任务 4 详情面板 + 任务 5 games collection ✓
- §3.6 timeline → 任务 5 timeline collection ✓
- §3.7 contact → 任务 5 contact collection ✓
- §4 视频增强 → 任务 1 + 任务 4 ✓
- §5 草稿开关 → 任务 2 + 任务 3 loader 过滤 + 任务 5 各 collection draft 字段 ✓
- §6 不纳入部分 → 计划不涉及 3D/季节/视觉 ✓
- §7 部署前置 → 任务 6 文档 ✓

**2. 占位符扫描：** 无 TODO/待定/类似任务 N，所有代码步骤含完整代码。

**3. 类型一致性：**
- `resolveVideo` 返回 `{type, src?}` 或 `{type, embed?}`，任务 4 `renderVideo` 消费 `v.src`/`v.embed` 一致 ✓
- `filterDraft` 签名全计划一致 ✓
- `validateGames` 扩展字段名 `video/description/metrics/draft/nda` 与 config.yml 字段名、games.json 字段名一致 ✓
- 目录索引 `index.json` 由 `scripts/gen-index.mjs` 生成，被 `data-loader.js` 的 `loadDir` 消费，格式（字符串数组）一致 ✓
